package Facts.structure;

/*Generated by MPS */

import java.util.List;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.LinkedList;

public enum ArticleType {
  de("de", "de"),
  het("het", "het"),
  een("een", "een"),
  geen("geen", "geen");

  private final String myName;
  public String getName() {
    return myName;
  }
  private final String myValue;
  private ArticleType(String name, String value) {
    myName = name;
    myValue = value;
  }
  public String getValue() {
    return myValue;
  }
  public String getValueAsString() {
    return myValue;
  }
  public static List<ArticleType> getConstants() {
    List<ArticleType> list = ListSequence.fromList(new LinkedList<ArticleType>());
    ListSequence.fromList(list).addElement(ArticleType.de);
    ListSequence.fromList(list).addElement(ArticleType.het);
    ListSequence.fromList(list).addElement(ArticleType.een);
    ListSequence.fromList(list).addElement(ArticleType.geen);
    return list;
  }
  public static ArticleType getDefault() {
    return ArticleType.de;
  }
  public static ArticleType parseValue(String value) {
    if (value == null) {
      return ArticleType.getDefault();
    }
    if (value.equals(ArticleType.de.getValueAsString())) {
      return ArticleType.de;
    }
    if (value.equals(ArticleType.het.getValueAsString())) {
      return ArticleType.het;
    }
    if (value.equals(ArticleType.een.getValueAsString())) {
      return ArticleType.een;
    }
    if (value.equals(ArticleType.geen.getValueAsString())) {
      return ArticleType.geen;
    }
    return ArticleType.getDefault();
  }
}
