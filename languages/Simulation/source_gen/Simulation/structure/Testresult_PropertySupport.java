package Simulation.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Testresult_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Testresult> constants = ListSequence.fromList(Testresult.getConstants()).iterator();
    while (constants.hasNext()) {
      Testresult constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Testresult> constants = ListSequence.fromList(Testresult.getConstants()).iterator();
    while (constants.hasNext()) {
      Testresult constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    Testresult constant = Testresult.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
